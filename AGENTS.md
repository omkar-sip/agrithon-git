# Agent Workspace Guide

Start navigation with `CODEBASE_TREE.md` before scanning the repository.

- Primary index: `CODEBASE_TREE.md`
- Refresh command: `powershell -ExecutionPolicy Bypass -File scripts/generate-codebase-tree.ps1`
- Scope note: the tree excludes `.git`, `node_modules`, and `.tmp` only.

If new folders or files are added, regenerate the tree first.
